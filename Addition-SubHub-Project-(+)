WelcomeTab = Window:CreateTab("Welcome", "handshake")

local Paragraph = WelcomeTab:CreateParagraph({Title = "Welcome To Operations SuperHub", Content = "Welcome to Operations SuperHub! This is a good script I developed. Each Operation has 5 games supported. For more information, go onto the info tab to see what scripts are supported and where its located :D"})

local AdditionTab = Window:CreateTab("Addition", "plus")

local Button = AdditionTab:CreateButton({
   Name = "Execute the Addition Script",
   Callback = function()
   
   end,
})
   

local SubtractionTab = Window:CreateTab("Subtraction", "minus")

local Button = SubtractionTab:CreateButton({
   Name = "Execute the Subtraction Script",
   Callback = function()
   -- The function that takes place when the button is pressed
   end,
})

local MultiplicationTab = Window:CreateTab("Multiplication", "x")

local Button = MultiplicationTab:CreateButton({
   Name = "Execute the Multiplication Script",
   Callback = function()
   -- The function that takes place when the button is pressed
   end,
})

local DivisionTab = Window:CreateTab("Division", "divide")

local Button = DivisionTab:CreateButton({
   Name = "Execute the Division Script",
   Callback = function()
   -- The function that takes place when the button is pressed
   end,
})

local InfoTab = Window:CreateTab("Info", "info")
